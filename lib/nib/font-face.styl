font-face($name, $file, $path = '', $weight = normal, $style = normal)
	$fontpath ?= '.'
	@font-face
		font-family $name
		font-weight $weight
		font-style $style
		src local('☺'), url($fontpath/$path/$file\.woff) format('woff')
